{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "s3-watcher.serviceAccountName" . }}
  labels:
    {{- include "s3-watcher.labels" . | nindent 4 }}
{{- end }}
